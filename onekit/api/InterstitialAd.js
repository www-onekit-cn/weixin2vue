/* eslint-disable class-methods-use-this */
export default class InterstitialAd {
  show() {}
  // 显示插屏广告。

  load() {}
  // 加载插屏广告。

  destroy() {}
  // 销毁插屏广告实例。

  onLoad() {}
  // 监听插屏广告加载事件。

  offLoad() {}
  // 取消监听插屏广告加载事件

  onError() {}
  // 监听插屏错误事件。

  offError() {}
  // 取消监听插屏错误事件

  onClose() {}
  // 监听插屏广告关闭事件。

  offClose() {}
  // 取消监听插屏广告关闭事件
}
